# Finished

| Prompt | Description | Date |
|--------|-------------|------|
| 0 | Task setup — created `/tasks` folder and NIM migration todo.md | 2026-06-12 |
| 1 | NIM provider migration (P0-P9) — added NIM as 4th provider (`nimClient` via `openai` SDK), 5 new model constants, `nim` branch in `executeOnProvider`, `nim → groq1 → gemini` fallback chain, migrated 11 ROUTES to NIM models, swapped `getVisionModel()` to NIM Llama 3.2 11B Vision with Gemini fallback, added `NVIDIA_API_KEY` to `.env` | 2026-06-12 |
| P0 | Phase 1 (v0.11) task tracking initialized — todo.md set up with P1-P6 plan for Data Foundation phase | 2026-06-14 |
| P1 | Database migrations 021-024 created and applied — investments, investment_transactions, net_worth_snapshots, pdf_import_jobs tables verified | 2026-06-14 |
| P2 | investments.js (5 endpoints: GET /, GET /summary, POST, PATCH :id, DELETE :id) with computed gain fields; analytics.js networth + investment-ratio endpoints; investmentAPI + analyticsAPI extended in frontend api.ts; tests passing (5 suites/32 tests) | 2026-06-14 |
| P3 | pdfImport.js route — POST /bank-statement (pdf-parse + AI extraction via new 'pdf-import' route), POST /bank-statement/:jobId/confirm (bulk insert in a transaction), GET /history; mounted at /api/import; importAPI added to frontend api.ts; tests passing (6 suites/36 tests) | 2026-06-14 |
| P4 | /investments page — portfolio summary StatTiles, holdings grouped by type with GROUP_LABELS, Add Investment + Update Price/Delete modals; types/investments.ts created; Briefcase nav entry added to Sidebar and BottomNav | 2026-06-14 |
| P5 | BankStatementImporter component — upload (drag/drop + recent imports), parsing (spinner), review (editable transaction table with type toggle/delete/add row), success steps; "Import PDF" button + Modal wired into /transactions page | 2026-06-14 |
| P6 | /net-worth page (hero, breakdown tiles, asset/liability detail, AreaChart history); NetWorthWidget on dashboard (sparkline + breakdown chips, conditional on accounts/investments); Investing-this-month StatTile via investment-ratio; "Net Worth" nav entry added to Sidebar and BottomNav. Phase 1 complete. | 2026-06-14 |
| - | Phase 1 complete. | 2026-06-14 |
| P0 | Phase 2 (v0.12) task tracking initialized — todo.md set up with P1-P7 plan for Wealth Intelligence phase | 2026-06-14 |
| P1 | Database migrations 025-027 created and applied — cams_import_jobs, tax_deductions, capital_gains_records tables verified | 2026-06-14 |
| P2 | camsImport.js route (POST /api/import/cams-statement + /:jobId/confirm) — pdf-parse + AI extraction via new 'cams-import' route in ai.js, upsert holdings into investments by ticker_or_folio, transactional confirm; migration 028 adds holdings_found column; mounted at /api/import alongside pdfImport; importAPI.uploadCams/confirmCamsImport added; tests passing (6 suites/36 tests) | 2026-06-14 |
| P3 | analytics.js — GET /wealth-velocity (last 8 net_worth_snapshots, MoM absolute/pct changes, avg growth, trend detection accelerating/decelerating/steady/insufficient_data) and GET /asset-allocation (bank + 8 investment types vs 60/25/5/10 equity-debt-gold-cash recommended model, deviation_score); analyticsAPI.getWealthVelocity/getAssetAllocation added; tests passing (6 suites/36 tests) | 2026-06-14 |
| P4 | tax.js route — getCurrentFY/fyToDateRange helpers; GET /80c-summary (totals, limit ₹150k, remaining, utilization_pct, breakdown_by_type, auto_add_candidates from ppf/elss investments), POST/PATCH/DELETE /80c CRUD with ownership checks, GET /capital-gains (FIFO buy/sell matching from capital_transactions, STCG/LTCG by equity 365d / other 1095d threshold); migrations 029 (tax_investments) + 030 (capital_transactions); TAX_INVESTMENT_TYPES + isValidFinancialYear added to validation.js; mounted at /api/tax; taxAPI added to frontend; tests passing (7 suites/43 tests) | 2026-06-14 |
| P5 | CamsImporter.tsx — 4-step modal (upload with drag/drop + CAS info note, parsing spinner, review table with per-holding delete + New/Update label, success with View portfolio / Import again); /investments page header gets "Import from CAMS" secondary button + Modal wired to fetchData() refresh on success; tsc --noEmit clean | 2026-06-14 |
| P6 | WealthVelocityWidget (avg monthly growth, trend badge accelerating/decelerating/steady/insufficient_data, mini bar chart of last 6 MoM changes) and AssetAllocationWidget (donut chart, actual vs target table, deviation alert) added to dashboard, gated on investments.length > 0; /wealth-intelligence page with full velocity chart + history table and full allocation breakdown + recommendation note; Sidebar and BottomNav get "Wealth Intelligence" entry (Sparkles icon); tsc --noEmit clean | 2026-06-14 |
| P7 | /tax page — FY selector (last 3 FYs); Section 80C Tracker (progress bar with green/amber/red utilization color, claimed/remaining/limit stat tiles, entries table with edit/delete, Add 80C modal, auto-add candidates from qualifying investments); Capital Gains Summary (4 stat tiles for STCG/LTCG equity/other, FIFO transactions table, empty state, taxation info note, Add transaction link); new /tax/add-transaction form page + POST /api/tax/capital-transaction backend route with FY computed from transaction date; CAPITAL_ASSET_TYPES/CAPITAL_TRANSACTION_TYPES validators added; "Tax" nav entry (Landmark icon) added to Sidebar and BottomNav More sheet; tsc --noEmit clean; backend tests passing (7 suites/43 tests). Phase 2 complete. | 2026-06-15 |
| - | Phase 2 complete. | 2026-06-15 |
| P1 | Migrations 031-032 created and applied — loans, loan_prepayments tables verified (CASCADE FKs); GET /api/analytics/networth updated to query real loans.outstanding_balance for total_loans_outstanding | 2026-06-15 |
| P2 | loans.js route — amortization engine (EMI formula, monthly schedule with prepayment application, invalid-config detection), GET/POST/PATCH/DELETE /api/loans (soft delete), GET /:id/amortization, POST+GET /:id/prepayments with months_saved/interest_saved computation; LOAN_TYPES + isValidLoanType added to validation.js; mounted at /api/loans; loanAPI added to frontend api.ts; tests passing (7 suites/43 tests) | 2026-06-15 |
| P3 | debt.js route — amortization engine extracted to utils/amortization.js (shared with loans.js); GET /payoff-optimizer (month-by-month avalanche/snowball cascade simulation with freed-EMI rollover, baseline vs strategy comparison, recommendation); GET /prepayment-impact (before/after schedule diff, penalty calc, net savings); GET /credit-utilization (per-card + aggregate, optimal/moderate/high/critical thresholds, paydown recommendation); GET /dti (3-month avg income, loan EMI + 5% card minimums, excellent/good/moderate/risky); mounted at /api/debt; debtAPI added to frontend; tests passing (7 suites/43 tests) | 2026-06-15 |
| P4 | /loans page — summary StatTiles (total outstanding, monthly EMI burden, weighted avg interest rate, active loan count); loan cards with type badge, progress bar (% principal repaid), edit/mark-repaid actions; expandable card with lazy-loaded amortization (12-row paginated table, cumulative interest LineChart, summary row) and prepayment list + Log Prepayment modal; Add Loan + Edit Loan modals; types/loans.ts created; "Debt" section with "Loans" entry (Building2 icon) added to Sidebar and BottomNav More sheet; tsc --noEmit clean | 2026-06-15 |
| P5 | /debt-intelligence page — DTI ratio card (income/EMI/card-minimum stat tiles + breakdown), credit utilization card (per-card progress bars with optimal/moderate/high/critical status colors + paydown recommendation), payoff optimizer (extra-payment input, avalanche vs snowball comparison panels with payoff sequences + recommendation), prepayment impact calculator (loan select + amount input, months/interest saved, penalty, net savings, old/new payoff dates); "Debt Intelligence" entry (Gauge icon) added to Sidebar and BottomNav Debt section; tsc --noEmit clean | 2026-06-15 |
| P6 | CreditUtilizationWidget (circular gauge colored green/amber/red by overall_utilization_pct, per-card list with last4 + status badges, recommendation note, link to /net-worth) and DtiWidget (large DTI ratio, status badge, thin colored bar, obligations vs income breakdown, "Add income transactions" empty state, link to /debt-intelligence) added to dashboard below AssetAllocationWidget; dismissible warning cards for utilization >50% (link to /net-worth) and DTI >35% (link to /debt-intelligence); net-worth page already showed real total_loans_outstanding from Prompt 1 (no change needed); tsc --noEmit clean, backend tests passing (7 suites/43 tests). Phase 3 complete. | 2026-06-15 |
| - | Phase 3 complete. Loans, amortization, payoff optimizer, prepayment calc, credit utilization, DTI — all live at v0.13. | 2026-06-15 |
| P0 | Phase 4 (v0.14) task tracking initialized — todo.md set up with P1-P8 plan for Planning Engine phase. Note: spec migrations 030-031 collide with existing 030_capital_transactions/031_loans — using 033-034 instead. | 2026-06-15 |
| P1 | Migrations 033-034 created and applied — scenarios (type CHECK investment_growth/loan_impact/expense_reduction/income_change, inputs_json/result_json JSONB, indexes on (user_id,created_at DESC) and (user_id,type)) and milestones (self-referential parent_id FK ON DELETE SET NULL, status CHECK not_started/in_progress/achieved/missed, indexes on user_id, (user_id,target_date), (user_id,status)) tables verified; valid parent_id insert succeeds, invalid parent_id raises FK violation | 2026-06-15 |
| P2 | planning.js — shared `simulateGrowth()` month-by-month compound growth utility; POST /fire (real-return-adjusted FIRE corpus/timeline with base/step-up-10%/extra-10k variants, PMT-based savings targets for 10/15/20yr, yearly portfolio projection, falls back to on-the-fly net worth calc if no snapshot exists); POST /sip (goal_based: PMT SIP amount + binary-searched step-up SIP + lumpsum PV + projection; growth_based: FV of annuity corpus + projection); GET /cashflow (12-month projection from 3-month income/expense averages + loan EMIs + active recurring expense outflows, surplus/healthy/tight/at_risk classification, running balance, summary); mounted at /api/planning; planningAPI added to frontend api.ts; backend tests passing (7 suites/43 tests), tsc --noEmit clean | 2026-06-15 |
| P4 | milestones.js route — GET / (self-join for parent_name, ordered by target_date ASC, inline feasibility object per milestone: days_remaining, months_remaining, amount_remaining, monthly_needed, is_on_track vs 3-month average savings, overdue flag); POST (validates name/target_date/target_amount, parent_id ownership check 404); PATCH /:id (full update, circular dependency guard via chainContains walking parent chain, 400 "This would create a circular dependency"); PATCH /:id/progress (current_amount/status, auto-sets status=achieved when current_amount===target_amount); DELETE /:id (hard delete, returns children_unlinked count via SET NULL cascade); MILESTONE_STATUSES + isValidMilestoneStatus added to validation.js; mounted at /api/milestones; milestoneAPI added to frontend api.ts; tests passing (8 suites/48 tests), tsc --noEmit clean | 2026-06-15 |
| P3 | Scenarios CRUD added to planning.js — GET /scenarios (lightweight list, excludes result_json), GET/:id (ownership-checked, full record), POST (validates SCENARIO_TYPES + non-empty inputs_json object), PATCH (updates title/inputs_json/result_json, bumps updated_at), DELETE (hard delete); SCENARIO_TYPES + isValidScenarioType added to validation.js. POST /scenarios/simulate runs 4 simulation types: investment_growth (compound growth via simulateGrowth, wealth ratio, yearly projection, summary_text), loan_impact (EMI calc, current vs new DTI using same logic as debt.js GET /dti, status change e.g. "excellent → excellent"), expense_reduction (annual savings + optional invested corpus), income_change (delayed-start compound growth with configurable savings rate). formatIndianCurrency() helper for L/Cr shorthand in summary_text. planningAPI extended with getScenarios/getScenario/createScenario/updateScenario/deleteScenario/simulate; backend tests passing (7 suites/43 tests), tsc --noEmit clean | 2026-06-15 |
| P6 | frontend/app/cash-flow/page.tsx — fetches planningAPI.getCashflow() on mount; summary banner (4 StatTiles: projected annual income/expenses, average monthly surplus colored by sign, months at risk with AlertTriangle); Recharts BarChart waterfall (12 monthly net_cashflow bars colored green/red via Cell, ReferenceLine at 0, custom tooltip with income/expenses/fixed outflows/net); Recharts LineChart running balance (line colored green/red depending on whether it ends higher or lower than it starts); 12-month projection table with per-row status background tinting (surplus/healthy/tight/at_risk) and signed colored net cash flow; Fixed Obligations card listing each active loan's EMI + recurring expenses total; collapsible Assumptions panel explaining income/expense computation and flat-projection caveat. Backend: GET /api/planning/cashflow now also returns loan_breakdown ([{id,name,emi}]) for the obligations list; tsc --noEmit clean, backend tests passing (8 suites/48 tests) | 2026-06-15 |
| P7 | frontend/app/scenarios/page.tsx — header with "New Scenario" button; saved scenarios list as cards (type icon/badge color-coded by type, created date, one-line preview from result_json.summary_text, delete icon), EmptyState when none; scenario builder rendered in a Modal (right-side panel desktop / full-page on mobile): Step 1 four type-selection cards (Investment Growth, Loan Impact, Expense Reduction, Income Change) with descriptions; Step 2 type-specific input forms (NumberField grid + redirect-to-investment checkbox for expense_reduction) and "Run Simulation" button calling planningAPI.simulate; Step 3 per-type result layouts (investment_growth: hero corpus + invested/returns/wealth-ratio tiles + projection AreaChart; loan_impact: hero EMI + interest/payable/payoff tiles + before/after DTI badges; expense_reduction: hero corpus-or-savings + savings tiles + optional projection chart; income_change: hero additional savings + corpus/invested/returns tiles + projection chart), all with summary_text callout card; Save Scenario opens a title-prefilled Modal calling planningAPI.createScenario, prepends to list with success toast. Backend: GET /api/planning/scenarios now also selects result_json so saved-card previews work; tsc --noEmit clean, backend tests passing (8 suites/48 tests) | 2026-06-15 |
| P8 | frontend/app/milestones/page.tsx — header with "Add Milestone" button; 3 StatTiles (total/on-track/achieved); recursive MilestoneNode dependency-tree renderer (root milestones at top level, children indented with a borderLeft connector + horizontal connector line per child); each card shows name, truncated description, target date/amount, status badge (not_started/in_progress/achieved/missed color-coded via STATUS_META), feasibility chip (On track / "Behind — needs ₹X/mo more" / Overdue), progress bar when target_amount > 0, edit/delete icon buttons; click expands to show full description, monthly_needed, and an inline "Log progress" panel (current_amount + status dropdown calling milestoneAPI.updateProgress); Add/Edit Milestone modal (name, target date, description, target/current amount, "Depends on" parent dropdown, priority) calling milestoneAPI.create/update; delete confirmation modal computes child count client-side and warns about unlinking before calling milestoneAPI.delete; EmptyState for no milestones. Navigation: added a "Planning" section (FIRE Calculator, SIP Optimizer → /fire, Cash Flow, Scenarios, Milestones) to Sidebar (refactored shared nav-item rendering into NavSectionLink) and a "Planning" group to BottomNav's More sheet; tsc --noEmit clean, backend tests passing (8 suites/48 tests). Phase 4 complete. | 2026-06-15 |
| - | Phase 4 complete. FIRE calculator, SIP optimizer, 12-month cash flow, 4-type scenario engine, milestone dependency planner — all live at v0.14. | 2026-06-15 |
| - | Phase 4 complete. | 2026-06-15 |
| P0 | Phase 5 (v0.15) task tracking initialized — todo.md set up with P1-P6 plan for Tax Intelligence phase. Note: spec migrations 032-034 collide with existing 032_loan_prepayments/033_scenarios/034_milestones — using 035-037 instead. | 2026-06-15 |
| P1 | Migrations 035-037 created and applied — tax_profiles (one row per user/FY, HRA/LTA/salary components, city_type, lta_claims_used_in_block, preferred_regime, itr_checklist_json JSONB), advance_tax_payments (4 installments/FY, cumulative_pct_due, unique on user/FY/installment), documents (vault metadata, storage_path UNIQUE, type CHECK, 3 indexes) tables verified; duplicate installment insert correctly fails unique violation | 2026-06-15 |
| P2 | tax.js extended — GET/POST /api/tax/profile (upsert on user_id+financial_year, defaults to current FY); GET /api/tax/hra (3-value-minimum exemption: actual HRA vs %-of-basic by city_type vs rent-10%-basic, limiting_factor label, optimization_potential capped at 40% of basic, plain-English explanation); GET /api/tax/lta (2022-25 block tracker, claims_remaining, months-to-block-end, recommendation when <12 months and claims remain). CITY_TYPES/TAX_REGIMES + isValidCityType/isValidTaxRegime added to validation.js. Fixed migration 035: preferred_regime VARCHAR(10)→VARCHAR(11) ('not_decided' is 11 chars). taxAPI.getProfile/saveProfile/getHra/getLta added to frontend; backend tests passing (8 suites/48 tests); smoke-tested both HRA limiting-factor branches via curl | 2026-06-15 |
| P3 | backend/src/utils/taxComputation.js — TAX_CONFIG keyed by FY (FY2024-25 post-Budget-2024 slabs: old regime 50k std deduction/12.5k 87A rebate up to 5L, new regime 75k std deduction/25k 87A rebate up to 7L, 4% cess), computeRegimeTax(taxableIncome, regime, fy) applying marginal slabs + rebate + cess (existing GET /api/ai/tax-estimate is AI-driven with no hardcoded slabs, left unchanged). tax.js: extracted computeHraExemption() shared helper (used by GET /hra and the new route); GET /api/tax/advance-tax (income estimate = max of YTD-transaction-extrapolation vs annualized profile salary, 80C+std-deduction+HRA deductions for old regime, regime comparison, 4-installment June/Sept/Dec/March schedule with cumulative %, paid/overdue/due/upcoming status, not-applicable when liability ≤₹10k); POST /api/tax/advance-tax/payment (upsert on user/FY/installment); GET/PATCH /api/tax/itr-readiness (4 auto items incl. capital_gains_computed and advance_tax_paid not_applicable branches, 4 manual toggle items stored in tax_profiles.itr_checklist_json, weighted score, completion_summary, next_action = highest-weight pending item). taxAPI.getAdvanceTax/logAdvanceTaxPayment/getItrReadiness/updateItrReadiness added to frontend; backend tests passing (8 suites/48 tests); smoke-tested full flow (profile→advance-tax estimate→log payment→status flips to paid→itr-readiness score updates→PATCH manual toggle) via curl | 2026-06-15 |
| P5 | frontend/app/fire/page.tsx — tab toggle (FIRE Calculator / SIP Optimizer); FIRE Calculator: on-mount computeFire({}) baseline, inputs (monthly expenses number, expected return/inflation/SWR sliders with SWR tooltip, extra savings input), Recalculate button; hero corpus_needed_real (DM Mono 52px), net-worth-vs-corpus ProgressBar, 3 scenario tiles (base/step-up/extra-10k) with earliest-FIRE-date highlighted border, 3 savings-target tiles (10/15/20yr); Recharts AreaChart portfolio projection with dashed corpus_needed_real ReferenceLine and ReferenceDot marking the FIRE-date crossing year, axis values abbreviated L/Cr via fmtAbbrev; SIP Optimizer: goal_based/growth_based mode toggle, debounced (400ms) computeSip on every input change (goal amount or monthly SIP, target years slider, return slider); goal_based hero shows monthly SIP needed + step-up SIP + lumpsum alternative, growth_based hero shows corpus at end; shared total invested/returns/wealth ratio tiles; stacked BarChart (invested vs returns per year) computed client-side from projection; tsc --noEmit clean, backend tests passing (8 suites/48 tests) | 2026-06-15 |
| P4 | backend/src/routes/documents.js — Supabase Storage (`fintrack-documents` bucket, service-role client) + `documents` table for the financial document vault. POST /api/documents (multer memory, 20MB limit, single 'file' field; validates name/type/financial_year; generates UUID, builds storage path `documents/{user_id}/{uuid}/{filename_with_underscores}`, uploads buffer with contentType, 502 on storage failure, else INSERTs metadata row and returns it); GET /api/documents (optional type/financial_year filters, ordered by created_at DESC, metadata only); GET /api/documents/:id/download-url (ownership check, createSignedUrl 1hr, returns download_url/expires_in_seconds/file_name/mime_type); DELETE /api/documents/:id (ownership check, removes from storage tolerating 404, always deletes DB row). DOCUMENT_TYPES + isValidDocumentType added to validation.js. SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY added to backend/.env. documentAPI (upload/getAll/getDownloadUrl/delete) added to frontend/lib/api.ts. backend/tests/documents.test.js (5 tests, mocks @supabase/supabase-js client); backend tests passing (9 suites/53 tests) | 2026-06-15 |
| P5 | frontend/app/tax/page.tsx — extended from 2 to 5 tabs (80C, Capital Gains unchanged; new HRA & LTA, Advance Tax, ITR Readiness) via a pill tab nav under the global FY selector, with lazy per-tab fetching. HRA & LTA tab: side-by-side (stacked on mobile) HRA exemption card (3 input tiles, exempt/taxable HRA, limiting-factor chip, optimization-potential callout, explanation text) and LTA block tracker card (entitlement, claims-remaining with circle badges, 2022—2025 timeline with current-position marker, amber recommendation card); collapsible "Salary Components" profile form (basic/HRA/LTA/special allowance/rent, Metro/Non-Metro toggle, LTA claims used 0-2, Old/New/Not-decided regime toggle) calling taxAPI.saveProfile then re-fetching HRA/LTA; "Set up profile" prompt card when no tax_profiles row exists. Advance Tax tab: not-applicable info card when liability ≤₹10k; else old/new regime comparison tiles with "Recommended" badge + savings line, estimated-income breakdown line, and an installment table (Paid/Overdue/Due Soon/Upcoming status chips, inline "Log Payment" row calling taxAPI.logAdvanceTaxPayment). ITR Readiness tab: SVG circular ScoreGauge (red/amber/green by score), completion_summary + next_action chip, Auto-checked items (lock icon, check/clock/dash status, hardcoded explanations) and Manual items (checkbox toggle calling taxAPI.updateItrReadiness with optimistic update). tsc --noEmit clean, backend tests passing (9 suites/53 tests) | 2026-06-15 |
| P6 | frontend/app/documents/page.tsx — Document Vault page. Header with Upload button; type filter (pill row, 9 DOCUMENT_TYPE_META entries + All) and FY filter (dropdown of FYs present in loaded docs + All years), both client-side via useMemo. Responsive document grid (Card per doc): type icon/color from DOCUMENT_TYPE_META, name, type badge, FY badge if set, formatFileSize, formatRelativeDate (with full date in title), Download (documentAPI.getDownloadUrl → window.open) and Delete (window.confirm → documentAPI.delete) icon buttons. Upload modal: click/drag-drop file zone (pdf/jpg/jpeg/png/xlsx, 20MB limit), name (prefilled from filename), type dropdown, FY dropdown (getFYOptions(5) + "Not applicable"), description, Upload button with isLoading spinner calling documentAPI.upload, prepends new doc on success, inline error display. EmptyState for zero documents; "No documents match your filters" + Clear filters for filtered-empty. Navigation: "Documents" entry (Archive icon) added to Sidebar navItems after Tax; BottomNav gets a new "Tax & Documents" More-sheet group (Tax moved out of Finance section, Documents added) to avoid duplicate Tax entries. Fixed a pre-existing bug where documentAPI.getAll used backslash path '\api\documents' (silently 404s) — corrected to '/api/documents'. tsc --noEmit clean, backend tests passing (9 suites/53 tests). Phase 5 complete. | 2026-06-15 |
| - | Phase 5 complete. HRA/LTA optimizer, advance tax schedule, ITR readiness score, document vault — all live at v0.15. | 2026-06-15 |
| - | Phase 5 complete. | 2026-06-15 |
| P0 | Phase 6 (v0.16) task tracking initialized — todo.md set up with P1-P7 plan for AI Financial OS phase. Note: spec migrations 035-037 collide with existing 035_tax_profiles/036_advance_tax_payments/037_documents — will use 038-040 instead. | 2026-06-15 |
| P1 | Migrations 038-040 created and applied — agent_conversations (4 agent_type CHECK, JSONB messages array, indexes on (user_id,agent_type,updated_at DESC) and (user_id,updated_at DESC)), opportunities (8 type CHECK, priority 1-3 CHECK, status active/dismissed/acted_on, indexes on (user_id,status,priority)/(user_id,detected_at DESC)/(user_id,type)), briefings (JSONB points, UNIQUE on (user_id,week_of), index on (user_id,week_of DESC)) tables verified; duplicate (user_id,week_of) insert correctly fails unique violation | 2026-06-15 |
| P2 | backend/src/routes/agents.js — 4 specialized AI agents (debt_coach, investment_advisor, tax_planner, budget_master), each with a dedicated DB data fetcher (loans/credit cards/DTI for debt_coach; portfolio/holdings/net-worth-history/FIRE target/asset-allocation for investment_advisor; tax profile/80C/capital gains/ITR readiness/advance tax for tax_planner; spending-by-category/budget adherence/savings rate/regretted transactions for budget_master) and a system-prompt builder injecting that data as plain-text context with persona + behavioral rules (₹-quantified, concrete next action, <200 words, ask clarifying questions). POST /message (new or continuing conversation via conversation_id, calls aiComplete('agent-chat'), persists to agent_conversations, auto-titles from first 60 chars), GET /conversations (list with message_count via jsonb_array_length, optional agent_type filter), GET/DELETE /conversations/:id (ownership-checked). Added 'agent-chat' route (nim/NEMOTRON_49B/1000/0.7) to ai.js ROUTES. Exported getCurrentFY/fyToDateRange/computeHraExemption/computeItrReadiness/computeAdvanceTaxEstimate/SECTION_80C_LIMIT from tax.js for reuse. agentAPI added to frontend lib/api.ts. Mounted at /api/ai/agent. tsc --noEmit clean, backend tests passing (9 suites/53 tests); smoke-tested full message/list/get/delete flow + invalid agent_type 400 via curl | 2026-06-15 |
| P3 | backend/src/routes/opportunities.js — opportunity detection engine with detectOpportunities(userId) running 8 detectors in parallel via Promise.all: idle_cash (bank balance > 4mo expenses, idle = balance - 3mo buffer, benefit = idle*5%), credit_card_interest (sum of outstanding card balances * 36% APR, priority 1), high_interest_loan (loans with rate>12%, interest saved ≈ outstanding*rate*yearsRemaining/2 via monthsRemainingForLoan, priority 1 if >18% else 2), tax_80c_gap (gap to ₹1.5L 80C limit from tax_investments, marginal rate 5/20/30% from tax_profiles salary components, expires_at = March 31 of current FY via fyToDateRange, priority 1 if <3 months left), spending_spike (category whose last month vs 3-month avg is >35% over, annualized), allocation_gap (bank+investments vs RECOMMENDED_PCT model, biggest >20% deviation, no amount_saved, priority 3), sip_underinvesting (investment ratio <15% of monthly income, 10-year 12% CAGR corpus delta), emergency_fund_low (bank balance < 3mo expenses, no amount_saved, priority 1). POST /detect upserts each result into opportunities by (user_id,type,status='active') — updates existing active row or inserts new, never auto-dismisses missing types; returns detected_count + active opportunities ordered by priority/detected_at. GET / returns active opportunities + dismissed/acted_on counts. PATCH /:id/dismiss and /:id/acted-on (ownership-checked, set status+timestamp). opportunityAPI (detect/getAll/dismiss/markActedOn) added to frontend lib/api.ts. Mounted at /api/ai/opportunities. tsc --noEmit clean, backend tests passing (9 suites/53 tests); smoke-tested detect→list→dismiss→re-detect→404-on-bad-id flow via curl against live DB (tax_80c_gap correctly detected for zero-investment profile) | 2026-06-15 |
| P4 | backend/src/routes/ai.js — getBriefingData(userId) aggregator runs 8 queries in parallel via Promise.all: net worth this week vs ~7 days ago (net_worth_snapshots), top spending category this month (transactions joined to categories), upcoming bills due in 7 days (recurring_transactions), this/last month savings rate (income/expense from transactions), top active opportunity (priority=1 first, then earliest detected_at), and investment/prepayment activity in the last 7 days (investment_transactions buys + loan_prepayments). buildBriefingPoints() turns this into 5 {label,value,insight} points (net_worth, top_spend, upcoming_bills, savings_rate, opportunity). generateWeeklyBriefing(userId): normalizes today to this week's Monday via mondayOf(), calls aiComplete('briefing', ...) for a 3-4 sentence narrative (with a non-AI fallback narrative if the call throws), derives action_of_the_week from the top opportunity's action_label or a positive default, upserts into briefings on (user_id,week_of), and sends an FCM push (title "Your Weekly Money Brief", body = first sentence of narrative) via sendToUser if the user has registered tokens, recording push_sent_at. POST /api/ai/briefing/generate (idempotent — ON CONFLICT overwrite), GET /api/ai/briefing/latest (404 if none, sets opened_at on first view), GET /api/ai/briefing/history (last 12, id/week_of/action_of_the_week/opened_at only). Added 'briefing' route (nim/MINIMAX_M27/300/0.6) to ai.js ROUTES. Cron: every Monday 8am IST (`cron.schedule('0 8 * * 1', ..., {timezone:'Asia/Kolkata'})`) added to index.js alongside the existing inline jobs — queries user_fcm_tokens, skips users who already have this week's briefing, calls generateWeeklyBriefing sequentially with 500ms delay, logs success/skipped/failed counts; generateWeeklyBriefing + mondayOf exported from ai.js routes for the cron to reuse. briefingAPI (generate/getLatest/getHistory) added to frontend lib/api.ts. tsc --noEmit clean, backend tests passing (9 suites/53 tests); smoke-tested generate→latest (opened_at set)→history flow via curl against live DB (AI narrative call failed/fell back gracefully to the deterministic fallback narrative — error handling worked as designed) | 2026-06-15 |
| P5 | backend/src/routes/insights.js — GET /api/insights/peer-benchmarks: 3-month avg monthly income from transactions → income bracket (under_5L/five_to_10L/ten_to_20L/above_20L by ₹41,667/83,333/166,667 monthly thresholds); last full calendar month's expenses grouped by category and mapped via GROUP_KEYWORDS (approximate name matching) into 8 hardcoded benchmark groups (food_dining, housing_rent, transport, entertainment_subscriptions, shopping_clothing, health_wellness, education, savings_investments); each group gets user_pct (vs that month's total income), benchmark_min/max from the hardcoded BENCHMARKS table (research-based ranges per bracket), status (below/within/above_benchmark), deviation_pct vs midpoint; savings_rate_comparison computed the same way against the savings_investments band; summary names the strongest area (most-below-benchmark spending category, excluding savings) and biggest overspend (most-above-benchmark). GET /api/insights/behavioral-patterns runs 5 detectors in parallel: budget_anchoring (categories with budgets in the last 3 months where spend lands in the 85-100% "near limit" zone in ≥2 of those months; detected if ≥2 such categories), present_bias (avg daily spend days 1-15 vs 16-end over the last 3 months, detected if first half >30% higher), subscription_bloat (transactions matching SUBSCRIPTION_KEYWORDS or entertainment/subscription categories with amount ₹100-2000, grouped by description+amount, detected if >5 recurring (2+ occurrence) groups), category_regret_concentration (is_regretted=true transactions grouped by category, detected if top category >40% of total), idle_savings_despite_debt (credit_cards.outstanding_balance sum > 0 AND bank balance via BANK_BALANCE_SQL > 2x outstanding). Detected patterns are passed to aiComplete('behavioral-insight', ...) to pick the single most impactful one, explain it in plain language, and give one actionable suggestion for the week (graceful fallback text if the AI call fails); returns {patterns, ai_insight, detected_count}. Added 'behavioral-insight' route (groq1/LLAMA70B/400/0.7) to ai.js ROUTES. Mounted at /api/insights (auth-protected via router.use(auth)). insightsAPI (getPeerBenchmarks/getBehavioralPatterns) added to frontend lib/api.ts. tsc --noEmit clean, backend tests passing (9 suites/53 tests); smoke-tested both endpoints against live DB with seeded data — peer-benchmarks correctly flagged Shopping & Clothing as the biggest overspend, behavioral-patterns correctly detected present_bias, subscription_bloat, category_regret_concentration (Shopping, 75%), and idle_savings_despite_debt (4/5 patterns), with AI insight correctly prioritizing the idle-savings-vs-debt pattern | 2026-06-15 |
| P6 | frontend/app/ai-advisor/page.tsx — new 4-agent advisor interface replacing /ai-chat. Left panel: "AI Advisor" header, 4 agent selector cards (Debt Coach/TrendingDown/--color-exp, Investment Advisor/LineChart/--color-inc, Tax Planner/Receipt/--color-warn, Budget Master/Wallet/--color-info), each with name + 1-line description; below the selected agent's cards, a "New conversation" link and a conversation history list (title + relative time via timeAgo()) fetched via agentAPI.getConversations(agent_type), collapsible into a full-width overlay on mobile (Menu/X toggle). Right panel: no-agent welcome screen with all 4 agents in a 2x2 grid (larger icons + longer descriptions); agent-selected-with-no-messages view showing agent name/description + 3 hardcoded starter questions per agent (clicking a starter sends it immediately); active-conversation view with right-aligned user bubbles (accent color) and left-aligned assistant bubbles tinted with the agent's accent color via color-mix, auto-scroll to bottom, and a 3-dot bounce typing indicator while sending. Chat input (textarea + send button, Enter-to-send, Shift+Enter for newline) shared across both conversation views. Send flow: optimistic user-message append, agentAPI.sendMessage(agent_type, message, conversation_id), replaces messages with the server's full message array on response, captures conversation_id on first response for a new conversation, and refetches the conversation list afterward so the sidebar picks up the new/updated title. All accent colors via CSS semantic variables (--color-exp/--color-inc/--color-warn/--color-info) and color-mix(), no hardcoded hex. Navigation: Sidebar and BottomNav "AI Chat" entries replaced with "AI Advisor" (Bot icon) at the same position, pointing to /ai-advisor; old /ai-chat/page.tsx replaced with a server-side redirect() to /ai-advisor. tsc --noEmit clean across the frontend | 2026-06-15 |
| P7 | frontend/app/dashboard/page.tsx — on mount, calls opportunityAPI.detect() and briefingAPI.getLatest() (added to the main data-fetching useEffect alongside existing analytics/AI calls). New "Your Weekly Brief" widget renders only when briefing.week_of matches the current week (computed client-side via a currentWeekOf useMemo replicating the backend's mondayOf() Monday-anchored logic): header + "Week of [Month Day]", first-2-sentences narrative truncation with "View full brief"/"Show less" toggle, 5 point chips from briefing.points (net_worth/top_spend/upcoming_bills/savings_rate/opportunity), and an amber Lightbulb-icon "This week: {action_of_the_week}" callout. New "Opportunities" section renders only when active opportunities exist: header with count badge, "See all" link to /insights (with ChevronRight) if >3 opportunities, up to 3 cards with borderLeft colored by priority (1→--color-exp, 2→--color-warn, 3→--text-faint), bold title, 2-line-clamped description (-webkit-box/WebkitLineClamp pattern), conditional "Save ₹X/year" success-tinted chip when amount_saved is non-null, and action/dismiss buttons — action button calls opportunityAPI.markActedOn(id) then router.push(action_route), dismiss button calls opportunityAPI.dismiss(id) with a 250ms fade-out (opacity/transform/transition) before removal from state. New frontend/app/insights/page.tsx (2 tabs): Tab 1 "Peer Benchmarks" fetches insightsAPI.getPeerBenchmarks() — income bracket pill, summary line, 8 category rows (icon via GROUP_ICONS, user_pct, custom BenchmarkBar range+marker visualization, "Benchmark: X%-Y%" text, status badge via statusBadge() using --color-inc/--color-info/--color-warn/--color-exp), plus a savings-rate comparison row with inverted badge logic (below benchmark = bad/red). Tab 2 "Behavioral Patterns" fetches insightsAPI.getBehavioralPatterns() — accent-tinted AI insight card with Brain icon, "X of 5 patterns detected" pill, 5-pattern list (PATTERN_LABELS human-readable names, CheckCircle2/AlertTriangle status icons, patternContext() helper formatting supporting_data per pattern type + description when detected, positive fallback text when not), and a Refresh button (Button variant="secondary" isLoading) re-calling getBehavioralPatterns(). Navigation: added "Insights" (Brain icon, already imported for /personality) to Sidebar navItems after AI Advisor, and to BottomNav's "AI & Insights" moreSections group after AI Advisor. tsc --noEmit clean across the frontend | 2026-06-15 |
| - | Phase 6 complete. 4 specialized AI agents with data injection, opportunity detection engine (8 detectors), weekly briefing with cron job, peer benchmarking, behavioral finance layer — all live at v0.16. | 2026-06-15 |
